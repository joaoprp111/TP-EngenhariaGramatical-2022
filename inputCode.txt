/* Teste das declaracoes */
list x = [1,2,3,4]; /* Podemos declarar listas */
int y = 3; /* Podemos declarar variáveis atómicas */
int z = y; /* Podemos declarar variáveis com outras variáveis */
int t = x[y]; /* Podemos declarar variáveis armazenadas nas estruturas */
double d = 2.31;
string s = "ola";
set conjunto = {2.3, 0.5, 1.45}; /* Podemos declarar conjuntos */
tuple tuplo = (True, False, True); /* Podemos declarar tuplos */
tuple tuploS = tuplo[1];
bool bo = True;
dict dicionario = {
  "quantidade" : 3,
  "soma": 2
}; /* Podemos declarar dicionários */
int do = dicionario["quantidade"];

input(b);

if((x + y) > x) {
  x = y + 2;
} else {
  y = x - 3;
}

if(x){
  if(y){
    print(y);
    if(x and z){
      input(x);
    }
  }
}

print(x + y);